# sentence-transformers >= 3.0 поддерживает bge-reranker-v2-m3 нативно через CrossEncoder.
# torch и transformers идут из базового pytorch/pytorch:2.6.0-cuda12.4-cudnn9 образа —
# НЕ дублировать здесь, иначе pip переустановит CPU-версию и сломает GPU.
sentence-transformers>=3.0
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
numpy
